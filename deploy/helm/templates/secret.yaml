apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kubernetes-mcp-server.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubernetes-mcp-server.labels" . | nindent 4 }}
type: Opaque
stringData:
  CLUSTER_REGISTRY: {{ .Values.clusterRegistry | quote }}
